base_url = "https://operad.dev"
title = "operad.dev"
description = "Notes on mathematics & programming — Luiz Mugnaini."
default_language = "en"

# Compile Sass from sass/ to public/.
compile_sass = true

# RSS / Atom feeds.
generate_feeds = true
feed_filenames = ["atom.xml"]

# No JS-based search; tags + the archive page are enough at this scale.
build_search_index = false

taxonomies = [
    { name = "tags", feed = true },
]

[markdown]
smart_punctuation = true

[markdown.highlighting]
theme = "tokyo-night"

[extra]
tagline = "Notes on mathematics & programming."
author = "Luiz Mugnaini"
wordmark = "operad.dev"
email = "luizmugnaini@gmail.com"
code_url = "https://git.sr.ht/~luizmugnaini"
available_for_hire = false

# Favicon — reused as the inline wordmark icon in the top bar.
favicon = "/favicon_knot-192x192.png"
favicon_mimetype = "image/png"

# OpenGraph / link-unfurl image. Path is relative to base_url; an absolute URL
# is built at render time by `get_url(path=..., trailing_slash=false)`.
# TODO: replace with a proper 1200×630 social card.
og_image = "/favicon_letter-512x512.png"
og_locale = "en_US"

# Theme colours used by mobile browser chrome. Mirror the SCSS palette.
theme_color_light = "#faf6ee"
theme_color_dark  = "#14141a"

# Top-level navigation.
# - Internal paths begin with `/` and are resolved against base_url at render time.
# - External links are flagged with `external = true` (open in a new tab).
menu = [
    { name = "notes", url = "/" },
    { name = "about", url = "/about" },
    { name = "links", url = "/links" },
    { name = "work",  url = "/work" },
    { name = "code",  url = "https://git.sr.ht/~luizmugnaini", external = true },
]
